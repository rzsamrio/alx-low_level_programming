/**
 * main - tests state of a number
 * @void: no inputs required
 * Return: 0
 */

#include <stdio.h>

int main(void)
{

	if (n == 0)
		printf("%d is 0\n", n);
	else if (n > 0)
		printf("%d is positive\n", n);
	else
		printf("%d is negative\n", n);
	return (0);
}
